name    = "mycelium"
summary = "Cross-repo ecosystem catalog for coding agents — scan mycelium.toml sidecars, merge a private overlay, serve one agent-readable catalog"
kind    = "tool"
status  = "wip"
tags    = ["catalog", "agents", "metadata", "cli"]
stack   = ["go"]

[[provides]]
name    = "catalog"
summary = "Serves the merged ecosystem map as CATALOG.md + catalog.json over HTTP"
